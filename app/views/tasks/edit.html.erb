<p><h1>Editing Tasks</h1></p>

<%= form_for @task do |f| %>
  <%= f.label :description %>
  <%= f.text_field :description %>

  <br>

  <%= f.submit "Update Task" %>

<% end %>

<%= link_to 'Show |', tasks_path(@task) %>
<%= link_to 'Back', tasks_path %>
