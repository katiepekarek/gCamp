<div class= 'page-header'>
  <h1>
    <% if @task.completed %>
      <s><%= @task.description %></s>
    <% else %>
      <%= @task.description %>
    <% end %>
    <%= link_to 'Edit', edit_task_path(@task), class:"btn btn-info pull-right" %>
  </h1>
</div>

<ol class="breadcrumb">
  <li><%= link_to 'Task', tasks_path %></a></li>
  <li class="active"><%= link_to @task.description %></li>
</ol>

<p>
  <% if @task.completed %>
    <s><%= @task.description %></s>
  <% else %>
    <%= @task.description %>
  <% end %>
</p>

<p>
  <strong><%= @task.due_date %></strong>
</p>
